### Auth

Auth is required for creation of new file sharings and also can be required to access some of the hoverever they can also public.

**Supported auth methods**
- OTP code send via mail/sms

In future: OAuth2 (Keyloack), Enteprise SAML etc.
